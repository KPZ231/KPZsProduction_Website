// Structured data for improved SEO
document.addEventListener('DOMContentLoaded', function() {
  // This file helps with schema.org JSON-LD implementation
  // The actual schema data is now included directly in each HTML file
  
  // This script can be used to dynamically generate structured data if needed in the future
  // For now, the static JSON-LD in each HTML file is sufficient for SEO purposes
}); 